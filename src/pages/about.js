import AboutMe from '../components/About/About';
import { Layout } from '../layout/Layout';

const About = () => {
    return (
        <Layout>
            <AboutMe />
        </Layout>
    );
};

export default About;
